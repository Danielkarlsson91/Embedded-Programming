/*Using BufferedSerial make a program for FRDM-K64F to read a string of max. 20 lowercase letters from terminal and
then reverse and convert the string to uppercase and print it to the terminal.
The read string from terminal shall only contain a to z.*/

#include <iostream>

int main()
{

    return 0;
}